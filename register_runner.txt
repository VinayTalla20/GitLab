
gitlab--dev  Tag

gitlab-runner register  --url https://gitlab-honknn  --token 

gitlab-

gitlab-runner register  --url https://gitlab-uujkb  --token 


gitlab runner setup type docker executer 

2. install docker
apt-get install docker.io -y

1. check arch of machine

uname -m
dpkg --print-architecture



2. Dowland and install binary


# Download the binary for your system
sudo curl -L --output /usr/local/bin/gitlab-runner https://gitlab-runner-downloads.s3.amazonaws.com/latest/binaries/gitlab-runner-linux-amd64

# Give it permission to execute
sudo chmod +x /usr/local/bin/gitlab-runner

# Create a GitLab Runner user
sudo useradd --comment 'GitLab Runner' --create-home gitlab-runner --shell /bin/bash

# Install and run as a service
sudo gitlab-runner install --user=gitlab-runner --working-directory=/home/gitlab-runner
sudo gitlab-runner start


3. register gitlab runner 



gitlab-runner register  --url https://gitlab-italent.com  --token 

sudo gitlab-runner register -n --url https://gitlab-italent.com \
		  --description "Docker Executor in iapease Tenant" \
          --executor "docker" \
          --docker-image "granucustomimages.azurecr.io/gitlab-dockerimage:latest-28-01-2025" \
		  --token glrt-t1_WR6Qvjzap6swgsn9xyrd \
		  --tag-list "gitlab-italent-dev" \
		  --run-untagged false \
		  --name "dev environments iapease"
		  
		  
		  
config.toml


concurrent = 15
check_interval = 0
connection_max_age = "15m0s"
shutdown_timeout = 0

[session_server]
  session_timeout = 1800

[[runners]]
  name = "gitlab-runner"
  url = "https://gitlab-doain.com/"
  id = 32
  token = "-K"
  token_obtained_at = 2025-02-19T06:53:16Z
  token_expires_at = 0001-01-01T00:00:00Z
  executor = "docker"
  [runners.custom_build_dir]
  [runners.cache]
    Type = "azure"
    Path = "gitlab-ci-cache"
    Shared = true
    MaxUploadedArchiveSize = 0
    [runners.cache.s3]
    [runners.cache.gcs]
    [runners.cache.azure]
    AccountName = "gitalbcache"
    AccountKey = ""
    ContainerName = "gitlab"
  [runners.docker]
    tls_verify = false
    image = "tlsexpimages.azurecr.io/gitlab-dockerimage:latest-28-01-2025"
    privileged = true
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    volumes = ["/var/lib/docker","/cache"]
    shm_size = 0
    network_mtu = 0
    pull_policy = "if-not-present"
		 
